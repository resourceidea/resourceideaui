@model IEnumerable<ResourceIdea.Pages.Clients._ClientEngagementsModel>

<div class="row">
    <div class="col">
        @if (Model is not null)
        {
            foreach (var item in Model)
            {
                <p>Client has engagements setup.</p>
            }
        }
        else
        {
            <p class="lead">Client has no engagements</p>
        }
    </div>
</div>
